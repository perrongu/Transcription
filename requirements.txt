# Transcription locale via faster-whisper
# Local transcription with faster-whisper
# Installation / Install: pip install -r requirements.txt

# Coeur / Core
faster-whisper>=1.0.0
tqdm>=4.66.0

# Interface web locale
fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
pytest>=7.4.0

# GPU NVIDIA (optionnel) / Optional NVIDIA GPU
# Si tu as une carte NVIDIA + CUDA, installe aussi PyTorch CUDA :
# If you have an NVIDIA GPU with CUDA, install PyTorch CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# Sinon ne touche à rien : ça tournera en CPU (plus lent, mais OK).
# Otherwise leave as-is: CPU will work (slower, but fine).
# Pour forcer l'install via ce fichier: enlève le '#' devant torch ci-dessous puis `pip install -r requirements.txt`.
# To force install via this file: remove the leading '#' on torch below, then run `pip install -r requirements.txt`.
